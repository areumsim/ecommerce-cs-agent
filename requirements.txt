# Core data + processing
datasets>=2.16.1
pandas>=2.1
pyarrow>=14.0.0
fastparquet>=2023.10.1

# Crawling / config
aiohttp>=3.9
PyYAML>=6.0.1

# API / UI
fastapi>=0.110
pydantic>=2.5
pydantic[email]>=2.5
uvicorn[standard]>=0.23
gradio>=4.0
pyvis>=0.3.2

# Authentication (Phase 3)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Monitoring (Phase 3)
prometheus-client>=0.17.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0

# Load Testing
locust>=2.20.0

# Structured Logging
structlog>=23.2.0

# LLM (선택 - SDK 설치 시 추가 기능 제공)
# openai>=1.0  # OpenAI SDK (타입 힌트, 스트리밍)
# anthropic>=0.18  # Anthropic SDK

# Fuseki/RDF (Graph RAG)
SPARQLWrapper>=2.0.0
rdflib>=7.0.0

# Optional (future)
# qdrant-client>=1.7
requests>=2.31
beautifulsoup4>=4.12
